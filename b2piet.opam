opam-version: "1.2"
version: "dev"

available: ocaml-version >= "4.03.0"
depends: [
  "atdgen"
  "batteries"
  "camlimages"
  "cmdliner"
  "conf-libpng"
  "integers"
  "lwt"
  "ocamlbuild" {build}
  "ppx_deriving"
  "qcheck" {test}
]
